[package]
name = "chesskom-cli"
description = "Terminal front-end for local two-player chess with rewind. Development/testing harness for chess-core ahead of the Kobo UI."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[[bin]]
name = "chesskom"
path = "src/main.rs"

[dependencies]
chess-core = { path = "../chess-core" }
